<div class="max-w-xl mx-auto bg-white p-6 rounded-lg shadow-md">
  <h1 class="text-2xl font-bold mb-4">プロフィール</h1>

  <div class="mb-2">
    <span class="font-semibold">名前:</span> <%= @user.name %>
  </div>

  <div class="mb-4">
    <span class="font-semibold">メールアドレス:</span> <%= @user.email %>
  </div>

  <%= link_to "編集する", edit_user_path(@user), class: "bg-blue-500 text-white px-4 py-2 rounded-lg hover:bg-blue-600 transition" %>
</div>
